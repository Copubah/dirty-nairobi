# Production Environment Configuration

# Database (Replace with your RDS endpoint)
DATABASE_URL=postgresql://username:password@your-rds-endpoint.amazonaws.com:5432/dirty_nairobi

# AWS Configuration (Replace with your actual values)
AWS_ACCESS_KEY_ID=your_actual_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_actual_aws_secret_access_key
AWS_REGION=us-east-1
S3_BUCKET_NAME=dirty-nairobi-photos-prod

# API Configuration
SECRET_KEY=your-super-secure-production-secret-key
API_V1_STR=/api/v1
PROJECT_NAME=Dirty Nairobi API

# CORS Origins (Replace with your actual domain)
BACKEND_CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com